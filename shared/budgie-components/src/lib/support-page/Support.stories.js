import { SignUpModal } from './Support';

export default {
  component: Support,
  parameters: {
    layout: 'fullscreen',
  },
};

export const Support = {
  render: () => <Support />, // this function specifies how to render a variant
};
